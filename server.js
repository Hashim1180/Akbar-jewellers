require('dotenv').config(); const express = require('express'); const http = require('http'); const { Server } = require('socket.io'); const mongoose = require('mongoose'); const cors = require('cors'); const apiRoutes = require('./src/routes/api'); const app = express(); const server = http.createServer(app); const io = new Server(server, { cors: { origin: '*' } }); app.set('io', io); app.use(cors()); app.use(express.json()); app.use('/api/v1', apiRoutes); io.on('connection', (socket) => { console.log('Vault Sec connection:', socket.id); socket.on('join_tracking', (orderId) => { socket.join(orderId); }); }); mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/akbar_vault').then(() => console.log('Vault DB connected')).catch(e => console.error(e)); server.listen(process.env.PORT || 5000, () => console.log('WeboraX Architect v24.0: Luxury Vault API is online'));