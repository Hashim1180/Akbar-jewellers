class DeliveryManager { constructor() { this.activeTransits = new Map(); this.states = ['VAULT_EXTRACTION', 'ARMORED_VEHICLE_LOADED', 'IN_TRANSIT_SECURE_ROUTE', 'FINAL_MILE_ESCORT', 'DELIVERED']; } startTracking(orderId, io) { if (this.activeTransits.has(orderId)) return; let index = 0; this.activeTransits.set(orderId, index); const interval = setInterval(() => { index++; if (index >= this.states.length) { clearInterval(interval); this.activeTransits.delete(orderId); io.to(orderId).emit('transit_update', { orderId, status: 'DELIVERED', message: 'Asset secured by client' }); return; } this.activeTransits.set(orderId, index); io.to(orderId).emit('transit_update', { orderId, status: this.states[index], timestamp: new Date().toISOString() }); }, 5000); } } module.exports = new DeliveryManager();