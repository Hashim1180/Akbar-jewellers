class WhatsAppNegotiator { constructor() { this.baseNumber = process.env.WA_NUMBER || '923034060500'; } generateNegotiationLink(productName, listedPrice, userOffer) { const numericPrice = parseInt(listedPrice.replace(/[^0-9]/g, '')); const numericOffer = parseInt(userOffer.replace(/[^0-9]/g, '')); const discountRatio = numericOffer / numericPrice; let message = ''; if (discountRatio >= 0.95) { message = 'Akbar Jewellers: I accept ' + userOffer + ' for [' + productName + ']. Forward wire instructions.'; } else { message = 'Akbar Jewellers: Regarding [' + productName + '] listed at ' + listedPrice + '. I propose ' + userOffer + '. Let us discuss.'; } return 'https://wa.me/' + this.baseNumber + '?text=' + encodeURIComponent(message); } } module.exports = new WhatsAppNegotiator();